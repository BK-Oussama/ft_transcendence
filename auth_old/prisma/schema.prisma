datasource db {
  provider = "postgresql"
  // url = env("DATABASE_URL") <--- REMOVE OR COMMENT THIS OUT
}

generator client {
  provider = "prisma-client" // Note: v7 prefers "prisma-client" over "prisma-client-js"
}