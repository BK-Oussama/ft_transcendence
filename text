instead of down all services, only rebuild and run a specific service.

docker compose up --build <service name>

example: docker compose up --build chat


export JWT_UNIT_TESTS="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEsImlhdCI6MTc3MzQ1OTAyOCwiZXhwIjoxNzczNDU5OTI4fQ.UCFAO68Y6_WQtRlNLASfM3Ux8oDPvaBURL96hUrUSAM"
export JWT_UNIT_TESTS=""


curl -k -X POST https://localhost/api/auth/login -H "Content-Type: application/json" -d '{"email":"oussama@1337.ma", "password":"SecurityFirst123"}'

curl -k -i -X POST https://localhost/api/auth/register \
     -H "Content-Type: application/json" \
     -d '{
       "email": "oussama@1337.ma",
       "password": "SecurityFirst123",
       "firstname": "Oussama",
       "lastname": "BK"
     }'




A script cannot change the environment of your current terminal unless you "source" the output. Run this immediately after:
- source .env_token