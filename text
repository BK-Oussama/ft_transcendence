Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

Datasource "db": PostgreSQL database "boards_db", schema "public" at "boards-db:5432"

The database is already in sync with the Prisma schema.

